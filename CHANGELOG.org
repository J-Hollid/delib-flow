#+title: delib-flow changelog

All notable repository changes should be recorded in this file.

The format is intentionally simple for now: reverse-chronological headings with
dated entries.

* 2026-04-28

- Continued Milestone 15 in `delib-flow.el`, `delib-flow-local-test-config.el`,
  `test/delib-flow-test.el`, `README.org`, and `docs/test-guide.org` by
  hardening the local-LLM path for `extract-actions`,
  `extract-waiting-for`, and `suggest-reference-notes` with compact prompts,
  array-tolerant parsing, bounded fallback behavior, stronger midpoint
  fixtures, and updated operator handoff guidance.
- Continued Milestone 15 in `delib-flow.el`, `test/delib-flow-test.el`,
  `README.org`, and `docs/test-guide.org` by adding filing-focused debug
  scenarios and filing-state seeding for deterministic stress testing:
  `filing-selection-mixed` and `filing-note-conflict`.
- Continued Milestone 15 in `delib-flow.el` and `test/delib-flow-test.el` by
  forcing debug checkpoint replay to use the deterministic local adapter even
  when the active manual-testing adapter is Ollama-backed, preventing later
  scenario checkpoints from hanging on hidden local-model replay.
- Continued Milestone 15 in `delib-flow-local-test-config.el` and
  `test/delib-flow-test.el` by salvaging truncated Ollama envelope responses
  for filing-review block stages, including `select-approved-filing-actions`,
  `reject-draft-filing-artifact`, and `resolve-filing-conflict`.

- Added midpoint-focused debug scenarios in `delib-flow.el`,
  `test/delib-flow-test.el`, `README.org`, and `docs/test-guide.org` with
  hardcoded strong `context-ready` state for downstream local-LLM stress
  passes: `action-rich-midpoint`, `waiting-rich-midpoint`, and
  `reference-note-rich-midpoint`.
- Updated `delib-flow-local-test-config.el`, `test/delib-flow-test.el`,
  `README.org`, and `docs/test-guide.org` so repo-local local-LLM scenario
  helpers can start at later supported checkpoints instead of forcing every
  manual pass to begin at `source`, including a direct
  `delib-flow-local-test-start-context-scenario` entrypoint for downstream
  stage hardening on slower machines.
- Added [[file:docs/test-guide.org][docs/test-guide.org]] as a dedicated local testing and debug
  operator guide covering walkthrough targets, direct scenario/checkpoint
  replay, inspection/comparison/audit commands, helper presets, and suggested
  manual verification passes.
- Continued Milestone 15 in `delib-flow.el`, `test/delib-flow-test.el`,
  `README.org`, and `docs/implementation-plan.org` by adding contextual
  helper presets for active manual review blocks so local verification can
  seed valid manual-project, cloud-failure, filing-selection, and
  filing-conflict decisions from the control buffer.
- Continued Milestone 15 in `delib-flow.el`, `test/delib-flow-test.el`,
  `README.org`, and `docs/implementation-plan.org` by adding an operator
  verification loop plus checkpoint-specific expectations to the walkthrough
  guide for named local verification targets.
- Continued Milestone 15 in `delib-flow.el`, `test/delib-flow-test.el`,
  `README.org`, and `docs/implementation-plan.org` by adding walkthrough
  step and restart commands so named local verification targets can advance
  between deterministic checkpoints from the control buffer.
- Continued Milestone 15 in `delib-flow.el`, `test/delib-flow-test.el`,
  `README.org`, and `docs/implementation-plan.org` by adding a walkthrough
  guide and starter flow for named local verification targets in the control
  buffer.
- Continued Milestone 15 in `delib-flow.el`, `test/delib-flow-test.el`,
  `README.org`, and `docs/implementation-plan.org` by adding a debug
  comparison view that diffs the active run against a fresh replay of its
  originating checkpoint and against the previous attempt of the latest stage.
- Continued Milestone 15 in `delib-flow.el`, `test/delib-flow-test.el`,
  `README.org`, and `docs/implementation-plan.org` by adding deterministic
  debug checkpoints for manual-project override, cloud-failure, and
  filing-conflict states, plus scenario/checkpoint compatibility validation.
- Continued Milestone 15 in `delib-flow.el`, `test/delib-flow-test.el`,
  `README.org`, and `docs/implementation-plan.org` by extending the debug
  inspection buffer with tracked state-change summaries plus stage-specific
  diagnostics for project matching, cloud routing, and filing.
- Started Milestone 15 in `delib-flow.el`, `test/delib-flow-test.el`,
  `README.org`, and `docs/implementation-plan.org` by adding built-in debug
  scenarios, checkpointed replay into major workflow phases, disposable temp
  fixture management, and a latest-stage inspection buffer for local manual
  verification.
- Completed Milestone 14 in `delib-flow.el`, `test/delib-flow-test.el`,
  `README.org`, and `docs/implementation-plan.org` by adding direct audit-log
  navigation from the control buffer, compact audit-navigation summary text,
  and regression coverage for jumping to the active run and latest persisted
  stage subtree.
- Planned Milestone 15 in `docs/implementation-plan.org` and `README.org` as
  a dedicated debug-framework phase for local verification, covering dummy-data
  end-to-end setup, stage-entry setup/replay, package/output diagnostics, and
  faster audit navigation for manual walkthroughs.
- Continued Milestone 14 in `delib-flow.el`, `test/delib-flow-test.el`,
  `README.org`, and `docs/implementation-plan.org` by adding stage-aware
  structured prompt guidance to stage input packages so resolved prompt
  content now carries semantic contracts for source analysis, project
  decisions, project creation, artifact drafting, cloud review, and filing
  review.
- Continued Milestone 14 in `delib-flow.el` and `test/delib-flow-test.el` by
  rendering resolved prompt-library/example content directly into stage input
  packages and by making persisted audit logs easier to follow with attempt
  numbers plus rerouted cloud transport-stage metadata.
- Started Milestone 14 in `delib-flow.el`, `test/delib-flow-test.el`,
  `README.org`, and `docs/implementation-plan.org` by adding policy-driven
  audit payload retention/redaction, richer audit status visibility in the
  control buffer, and prompt-library/example-structure resolution in stage
  input packages.
- Completed the remaining Milestone 13 UI/control decision in
  `delib-flow.el`, `test/delib-flow-test.el`, `README.org`, and
  `docs/implementation-plan.org`: the control buffer now foregrounds rerouted
  cloud target stages in current-result and failure-recovery surfaces while
  keeping `Run Cloud Stage` as the explicit reviewed transport boundary.
- Continued the Milestone 13 generic cloud-rerouting pass in
  `delib-flow.el` and `test/delib-flow-test.el`, adding direct retry of the
  current rerouted cloud target from the approved reviewed package plus a
  one-way `Restart Cloud Path` reset that returns the run to sanitization
  without supporting arbitrary backtracking across cloud substeps.
- Continued the Milestone 13 generic cloud-rerouting pass in
  `delib-flow.el` and `test/delib-flow-test.el`, promoting rerouted cloud
  target stages into first-class cloud stage-history records and recording
  rerouted cloud failures against the actual target stage while keeping local
  stage legality gated on explicit reintegration.
- Continued the Milestone 13 generic cloud-rerouting pass in
  `delib-flow.el`, `test/delib-flow-test.el`, `README.org`, and
  `docs/implementation-plan.org`, so reviewed cloud execution now preserves
  the selected rerouted stage's real payload and approved reintegration now
  applies that payload through the target stage's normal local state-update
  path before downstream filing review resumes.

* 2026-04-27

- Started the Milestone 13 generic cloud-rerouting pass in `delib-flow.el`
  and `test/delib-flow-test.el`, adding explicit reviewed cloud target-stage
  selection, recorded `:cloud-target-stage` routing state, a dedicated
  cloud-routing review block, and cloud-stage output that now reports which
  rerouted reasoning stage the reviewed package was sent through.
- Completed the Milestone 12 cloud-failure and fallback workflow in
  `delib-flow.el`, `test/delib-flow-test.el`, `README.org`, and
  `docs/implementation-plan.org`, adding explicit cloud-failure review state,
  a `Resolve Cloud Failure` stage with retry/local-fallback/skip/abort
  outcomes, and documentation that now treats milestone 12 as implemented and
  moves active roadmap focus to Milestone 13.
- Added a post-milestone roadmap to `docs/implementation-plan.org` covering
  the remaining backlog after milestones 5 through 8, and started Milestone 9
  work on proposal-quality refinement.
- Refined proposal drafting in `delib-flow.el` and `test/delib-flow-test.el`
  so action extraction now prefers concrete source-body action lines,
  waiting-for extraction can reuse explicit waiting lines from the source, and
  source-derived reference-note suggestions prefer project-support notes when a
  matched project already exists.
- Continued the Milestone 9 proposal-quality pass in `delib-flow.el` and
  `test/delib-flow-test.el` so retained reference material can now contribute
  concrete action lines, explicit waiting lines, and note-worthy support-note
  focus text instead of relying only on retained note titles.
- Started the next Milestone 9 validation slice in `delib-flow.el` and
  `test/delib-flow-test.el`, adding warning severity, surfacing blocking
  warning counts in proposal output and filing preview, and preventing filing
  approval of artifacts that are not yet deterministically file-ready.
- Continued the Milestone 10 filing-review pass in `delib-flow.el` and
  `test/delib-flow-test.el`, persisting blocked approval attempts into filing
  state, rendering explicit blocked-artifact recovery details in `Filing
  preview`, and clearing stale block state when new filing drafts are generated.
- Completed the Milestone 10 validation-and-approval-gates pass in
  `delib-flow.el`, `test/delib-flow-test.el`, `README.org`, and
  `docs/implementation-plan.org`, adding per-artifact readiness/remediation
  guidance, broader waiting-for blocking validations, ready-versus-blocked
  selection guidance, and documentation that now treats milestone 10 as
  implemented.
- Started the Milestone 11 filing-conflict-resolution pass in
  `delib-flow.el`, `test/delib-flow-test.el`, and `README.org`, adding an
  executable `Resolve Filing Conflict` stage, explicit retry/reject/rename-note
  recovery paths, a dedicated conflict-resolution review block in `Filing
  preview`, and test-covered reference-note retargeting after deterministic
  conflicts.
- Extended the Milestone 11 filing-conflict-resolution pass in
  `delib-flow.el` and `test/delib-flow-test.el` so deterministic conflict
  recovery now also supports project-child rewording and top-level project
  retitling before retrying the existing filing stage.
- Updated `docs/implementation-plan.org` and `README.org` after the Milestone
  11 audit pass so the repo now treats conflict resolution as implemented for
  the current deterministic filing targets and moves the active roadmap focus
  to Milestone 12.
- Updated `README.org` and `docs/implementation-plan.org` after a Milestone 8
  audit pass to mark the reviewed cloud-routing path as implemented with
  explicit definition of done, implementation anchors, remaining gaps, and
  test-backed exit criteria.
- Updated `README.org` and `docs/implementation-plan.org` after a Milestone 7
  audit pass to mark deterministic filing as implemented with explicit
  definition of done, implementation anchors, non-goals, follow-on work, and
  test-backed exit criteria.
- Tuned the remaining Milestone 5 proposal heuristics in `delib-flow.el` and
  `test/delib-flow-test.el`, replacing the weakest action and waiting-for
  placeholder wording with stronger default scaffolding and owner-aware
  waiting-for drafts.
- Updated `README.org` and `docs/implementation-plan.org` after a Milestone 6
  audit pass to mark audit logging as implemented with explicit definition of
  done, non-goals, implementation anchors, and test-backed exit criteria.
- Implemented the Milestone 5 proposal-review refinement pass in
  `delib-flow.el` and `test/delib-flow-test.el`, adding structured quality
  warnings for drafted actions, waiting-for items, and reference notes, plus
  operator-selective approve and reject review over draft filing artifacts.
- Updated `README.org` and `docs/implementation-plan.org` to mark Milestone 5
  as implemented and to move remaining work into post-milestone heuristic and
  ergonomics follow-up.
- Implemented the locked-in Milestone 2 hardening pass in `delib-flow.el` and
  `test/delib-flow-test.el`, so deterministic project-support note filing now
  also updates the matched project's `REFERENCE_FILES` metadata with a link to
  the newly created support note.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect the narrowed but real project
  metadata update path and to mark the fourth locked-in Milestone 2
  refinement pass as implemented.
- Implemented the locked-in Milestone 2 cloud-policy pass in `delib-flow.el`
  and `test/delib-flow-test.el`, adding provider-aware cloud-routing policy
  checks, recorded provider/policy-profile state, and strict-profile
  sanitization that redacts Org file links and project-identifying terms.
- Fixed the new cloud-policy routing update path in
  `delib-flow--apply-decide-cloud-pass-entry` and tightened strict
  sanitization so project-term redaction does not match inside its own
  placeholder markers.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect provider-aware cloud policy and
  policy-profile-aware sanitization.
- Added `scripts/load-delib-flow-tests.el` so an interactive Emacs session can
  load both `delib-flow.el` and `test/delib-flow-test.el` before running
  `ert-run-tests-interactively`.
- Implemented the locked-in Milestone 2 capture-template pass in
  `delib-flow.el` and `test/delib-flow-test.el`, routing deterministic
  reference-note creation through configurable package templates with
  package-provided defaults for general and project-support notes.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect template-backed note creation.
- Implemented the locked-in Milestone 2 discovery-refinement pass in
  `delib-flow.el` and `test/delib-flow-test.el`, so reference discovery now
  prioritizes directly linked project notes and uses project tags and contact
  addresses as first-class retrieval signals.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect that discovery is no longer a
  plain weighted-term pass.
- Recorded the locked-in Milestone 2 refinement sequence in
  `docs/implementation-plan.org`: discovery heuristics, capture-template-backed
  note filing, cloud policy refinement, and a final hardening pass, with scope
  allowed to change only if new implementation information justifies it.
- Implemented metadata-aware project matching in `delib-flow.el` and
  `test/delib-flow-test.el`, expanding candidate parsing and scoring to use
  project heading tags, subtree terms, and contact addresses in addition to
  title overlap.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect that project matching is no longer
  title-only.
- Implemented deterministic filing conflict detection in `delib-flow.el` and
  `test/delib-flow-test.el`, so filing now preflights duplicate project-child,
  project-heading, and reference-note targets, preserves the approved artifact
  for retry, and surfaces conflict state in `Filing preview`.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect that filing conflict detection is
  now part of the implemented Milestone 2 workflow.
- Added end-to-end Milestone 2 workflow coverage in
  `test/delib-flow-test.el` for both the local matched-project path and the
  cloud-assisted approval path.
- Updated `README.org` and `docs/implementation-plan.org` to treat Milestone 2
  as a complete core workflow with remaining refinement work called out as
  heuristic and integration limits rather than missing stage coverage.
- Implemented the next Milestone 2 slice in `delib-flow.el` and
  `test/delib-flow-test.el`, adding an end-to-end `Approve Candidate
  Reintegration` stage so cloud-returned output must be explicitly approved
  before local reintegration.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect the reintegration-approval stage
  and the current Milestone 2 stage count.
- Implemented the next Milestone 2 slice in `delib-flow.el` and
  `test/delib-flow-test.el`, adding an end-to-end `Approve Cloud Send` stage
  and an editable reviewed cloud-package surface in the control buffer.
- Refactored stage-command rerendering so completed stages refresh the control
  buffer without first resyncing stale pre-stage editable text back into run
  state.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect explicit cloud-send approval and
  reviewed-package editing before cloud execution.

* 2026-04-26

- Synchronized `CHANGELOG.org` with the current Milestone 2 implementation
  state before the next planning pass, including the explicit
  one-artifact-at-a-time filing review loop.
- Implemented the next Milestone 2 slice in `delib-flow.el` and
  `test/delib-flow-test.el`, adding an end-to-end `Select Approved Filing
  Actions` stage so filing approval is explicit and one item at a time.
- Refactored the filing review path so `Integrate into Source` preserves draft
  artifacts for review instead of auto-approving them, and `File Approved
  Outputs` now consumes only the current approved artifact before clearing the
  approval slot.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect the explicit filing-selection
  stage and one-at-a-time filing behavior.
- Replaced the stub `docs/design.org` with a repo-local copy of the canonical
  design note.
- Added `docs/implementation-plan.org` to separate execution planning from the
  design contract.
- Added `CHANGELOG.org`.
- Updated `README.org` to point to the design document, implementation plan,
  and changelog.
- Updated `docs/implementation-plan.org` with agreed planning decisions:
  shared adapter recommendation, direct in-control-buffer editing, and
  audit-before-filing milestone order.
- Refined Milestone 1 in `docs/implementation-plan.org` into concrete
  workstreams for run state, control-buffer regions, lifecycle, action
  legality, rendering behavior, and test coverage.
- Recorded the agreed Milestone 1 editing representation: package-managed
  headings with explicit editable blocks inside the control buffer.
- Added the accepted control-buffer contract recommendations and a concrete
  section schema to `docs/implementation-plan.org`.
- Added accepted literal internal schema recommendations for run state,
  editable blocks, valid actions, and anchor lookup to
  `docs/implementation-plan.org`.
- Added a code-facing Milestone 1 breakdown mapping planned work onto
  `delib-flow.el` and `test/delib-flow-test.el`.
- Added enforced project constraints to `docs/implementation-plan.org`:
  validated test coverage of at least 90 percent and validated cyclomatic
  complexity of at most 5.
- Added an operational validation design to `docs/implementation-plan.org`
  covering `ERT`, coverage gating, complexity gating, and `make` command
  expectations.
- Added an exact Slice A plan to `docs/implementation-plan.org` covering the
  first run-state refactor and baseline test expansion.
- Implemented Slice A in `delib-flow.el` and `test/delib-flow-test.el`,
  including the new run-state schema helpers and expanded baseline tests.
- Added `Makefile`, `scripts/validate-coverage.el`, and
  `scripts/validate-complexity.el`.
- Refactored section rendering so the codebase passes the enforced cyclomatic
  complexity gate.
- Implemented Slice B in `delib-flow.el` and `test/delib-flow-test.el`,
  including structured action objects, explicit refresh and abort commands,
  action rendering from state, and one-active-run enforcement.
- Implemented Slice C in `delib-flow.el` and `test/delib-flow-test.el`,
  including removal of `view-mode`, direct-editable control-buffer rendering,
  and explicit editable-block scaffolding in the planned sections.
- Implemented Slice D in `delib-flow.el` and `test/delib-flow-test.el`,
  including editable-block state syncing and refresh preservation.
- Implemented Slice E in `delib-flow.el` and `test/delib-flow-test.el`,
  including managed-region protection, conflict detection, and
  section-by-section refresh behavior.
- Updated `README.org` and `docs/implementation-plan.org` to reflect Milestone
  1 outcomes, current status, and implementation learnings.
- Updated `docs/design.org` with a Milestone 1 implementation-status note and
  design-level learnings confirmed by the current control-buffer foundation.
- Implemented the first Milestone 2 slice in `delib-flow.el` and
  `test/delib-flow-test.el`, adding a generic local stage execution contract
  and an end-to-end `Inspect Source` stage.
- Updated `README.org` and `docs/implementation-plan.org` to reflect Milestone
  2 execution-contract progress and learnings.
- Implemented the next Milestone 2 slice in `delib-flow.el` and
  `test/delib-flow-test.el`, adding an end-to-end `Match Project` stage backed
  by deterministic My Projects matching and stage-dependent follow-up actions.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect the current Milestone 2 state.
- Implemented the next Milestone 2 slice in `delib-flow.el` and
  `test/delib-flow-test.el`, adding an end-to-end `Discover Relevant Reference
  Material` stage backed by deterministic local retrieval from the configured
  ZK root and discovery-dependent follow-up actions.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect the current discovery-stage state.
- Implemented the next Milestone 2 slice in `delib-flow.el` and
  `test/delib-flow-test.el`, adding an end-to-end `Filter Useful Reference
  Material` stage backed by deterministic filtering over retrieved candidates.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect the current filtering-stage state.
- Implemented the next Milestone 2 slice in `delib-flow.el` and
  `test/delib-flow-test.el`, adding an end-to-end `Extract Actions` stage
  backed by deterministic local drafting into filing preview.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect the current action-extraction
  state.
- Implemented the next Milestone 2 slice in `delib-flow.el` and
  `test/delib-flow-test.el`, adding an end-to-end `Extract Waiting-For` stage
  backed by deterministic local drafting into filing preview.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect the current waiting-for extraction
  state.
- Implemented the next Milestone 2 slice in `delib-flow.el` and
  `test/delib-flow-test.el`, adding an end-to-end `Suggest Reference Notes`
  stage backed by deterministic local drafting into filing preview.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect the current reference-note
  suggestion state.
- Implemented the next Milestone 2 slice in `delib-flow.el` and
  `test/delib-flow-test.el`, adding an end-to-end `Decide on Cloud Pass`
  stage backed by deterministic local routing-state updates.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect the current cloud-routing decision
  state.
- Implemented the next Milestone 2 slice in `delib-flow.el` and
  `test/delib-flow-test.el`, adding an end-to-end `Sanitize for Cloud` stage
  backed by deterministic local redaction into working context.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect the current cloud-sanitization
  state.
- Implemented the next Milestone 2 slice in `delib-flow.el` and
  `test/delib-flow-test.el`, adding an end-to-end `Run Cloud Stage` step
  through the dedicated cloud adapter path and an end-to-end `Integrate into
  Source` stage for local reintegration and approval-state promotion.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect the current cloud-execution and
  reintegration state.
- Implemented the next Milestone 2 slice in `delib-flow.el` and
  `test/delib-flow-test.el`, adding an end-to-end `File Approved Outputs`
  stage for deterministic project-child insertion and reference-note creation.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect the current deterministic filing
  state.
- Implemented the next Milestone 2 slice in `delib-flow.el` and
  `test/delib-flow-test.el`, adding incremental audit-log state updates,
  configured audit-file persistence, and control-buffer audit status rendering.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect the current audit logging state.
- Implemented the next Milestone 2 slice in `delib-flow.el` and
  `test/delib-flow-test.el`, adding an end-to-end `Propose New Project` stage
  and deterministic filing support for approved project proposals.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect the current no-match project
  creation state.
- Implemented the next Milestone 2 slice in `delib-flow.el` and
  `test/delib-flow-test.el`, adding an end-to-end `Choose Project Manually`
  stage and retryable manual-selection behavior on ambiguous or no-match
  branches.
- Updated `README.org`, `docs/design.org`, and
  `docs/implementation-plan.org` to reflect the current manual project
  selection state.
